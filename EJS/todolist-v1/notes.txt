<%= this is called marker %>

<%=   Equal sign(=) outputs the value to the template(HTML escaped)

<% 'Scriptlet' tag. for control-flow, no output. It allows us to use JavaScript code in a EJS template.
    Works line by line! Which means you have to put that tag to EVERY LINE of the JavaScript code!

